# formatters/

从 ast/ 模块重导出，用于向后兼容。新代码请直接使用 ast/。

## 文件

| 文件          | 内容                    | 何时阅读             |
| ------------- | ----------------------- | ------------------------ |
| `__init__.py` | 从 ../ast/ 重导出 | 查看可用类型时 |

实际实现请参见 `../ast/CLAUDE.md`。
